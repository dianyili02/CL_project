def get_direction(pos, goal):
    dx = goal[0] - pos[0]
    dy = goal[1] - pos[1]

    if dx > 0:
        return 1  # Down
    elif dx < 0:
        return 0  # Up
    elif dy > 0:
        return 3  # Right
    elif dy < 0:
        return 2  # Left
    else:
        return 4  # Stay
